
name: publish documentation

on:
  push:
    tags:
      - '*'
  workflow_dispatch:

jobs:
  build-doc:
    name: Build documentation
    runs-on: ubuntu-latest

    steps:
    - name: Regular checkout
      uses: actions/checkout@v4
      with:
        ref: doc
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - uses: actions/setup-node@v4
      with:
        cache-dependency-path: docs/package-lock.json
        node-version: 22
        cache: npm
    - name: Install dependencies
      working-directory: docs
      run: npm ci
    - name: List files in docs
      working-directory: docs
      run: ls
    - name: List files in node_modules
      working-directory: docs/node_modules
      run: ls
    - name: List files in node_modules/@easyops-cn
      working-directory: docs/node_modules/@easyops-cn
      run: ls
    - name: Build website
      working-directory: docs
      run: npm run build

    - name: Merge and push
      run: |
        echo "Auto-merge ${{ github.ref_name }} to doc branch"
        git config --global user.name 'github-actions'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        git fetch --tags origin
        git add docs/build -f
        git merge ${{ github.ref_name }} -m "Auto-merge ${{ github.ref_name }} to doc branch"
        git push