---
openapi: 3.0.3
info:
  title: My Project
  version: 10.5.36
servers:
  - url: ""
tags:
  - name: FileUploadController
paths:
  /api/file-uploadupload-files:
    post:
      tags:
        - FileUploadController
      operationId: uploadFiles
      requestBody:
        content:
          multipart/form-data:
            schema:
              required:
                - files
      responses:
        200:
          description: successful operation
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
